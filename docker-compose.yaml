version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: seb
      POSTGRES_USER: seb
      POSTGRES_DB: would-you-rather